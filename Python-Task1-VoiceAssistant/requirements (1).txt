requests>=2.31.0

# Optional but recommended for voice input/output.
# If these fail to install (PyAudio can be tricky on some systems),
# the assistant will automatically fall back to typed text input,
# so the app still works fully without them.
speechrecognition>=3.10.0
pyttsx3>=2.90
pyaudio>=0.2.13
